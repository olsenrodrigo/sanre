/**
 * O WhatsApp agora vive dentro da Assistente Sanrê (widget único no canto da
 * tela, montado em App.tsx). Mantido vazio só para as telas antigas que ainda
 * importam o componente não quebrarem.
 */
export default function WhatsAppFloat() {
  return null;
}
